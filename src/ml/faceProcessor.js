import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function faceProcessor() {
  return (
    <View>
      <Text>faceProcessor</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
